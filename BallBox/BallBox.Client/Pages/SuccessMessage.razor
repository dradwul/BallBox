@if (ShowMessage)
{
    <div class="alert alert-success">
        @Message
    </div>
}

@code {
    [Parameter]
    public string Message { get; set; }

    [Parameter]
    public bool ShowMessage { get; set; }
}